import React from "react";

const ChatLayout = ({ children }: { children: React.ReactNode }) => {
  return <div className="ax-autp max--w-4xl h-full w-full">{children}</div>;
};

export default ChatLayout;
